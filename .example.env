# TORA Environment Variables
# Bu dosyayı kopyalayıp .env olarak kaydedin ve değerleri doldurun
# cp .example.env .env

# ============================================
# Database Configuration
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tora
DB_USER=postgres
DB_PASSWORD=postgres

# ============================================
# LDAP Configuration
# ============================================
# LDAP'yi etkinleştir/devre dışı bırak (true/false)
# false yaparsanız sadece local user'lar kullanılır
# NOT: Bu ayar hala environment variable olarak kullanılıyor
LDAP_ENABLED=true

# ============================================
# JWT Configuration
# ============================================
# JWT secret key (minimum 256 bit - production'da mutlaka değiştirin!)
# Güvenli bir secret oluşturmak için: openssl rand -base64 32
# UYARI: Default değer bırakılırsa ENFORCE_SECRET_VALIDATION=true olan ortamlarda uygulama başlamaz!
JWT_SECRET=your-secret-key-change-this-in-production-min-256-bits

# Encryption key (LDAP şifrelerini şifrelemek için, min 32 karakter)
ENCRYPTION_KEY=default-encryption-key-change-in-production-min-32-chars-long

# KDF salt (LDAP şifre türetmesi için, production'da min 16 karakter rastgele değer)
# openssl rand -base64 16
ENCRYPTION_SALT=Tora-v2

# Production'da aşağıdakini true yapın — default secret kullanımını önler
# Dev/local ortamda false bırakın
ENFORCE_SECRET_VALIDATION=false

# JWT token expiration süresi (milisaniye cinsinden)
# Varsayılan: 86400000 (24 saat)
# Test için: 60000 (1 dakika)
JWT_EXPIRATION=60000

# ============================================
# Server Configuration
# ============================================
# Backend server port
SERVER_PORT=8080

# ============================================
# Logging Configuration
# ============================================
# Log seviyesi (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# Security log seviyesi
SECURITY_LOG_LEVEL=DEBUG

# SQL sorgularını logla (true/false)
SHOW_SQL=false

# ============================================
# Frontend Configuration (Vite)
# ============================================
# Frontend API base URL (development için genelde /api yeterli)
# Production'da tam URL gerekebilir: https://api.example.com
VITE_API_BASE_URL=/api

# ============================================
# Sample Data Seeding
# ============================================
# Örnek veri eklemek için 1 yapın (her ekibe 5 kullanıcı, 2025 yılı için işler ve projeler)
# 0 = Kapalı, 1 = Açık
SEED_SAMPLE_DATA=0

# ============================================
# Docker Compose Override (opsiyonel)
# ============================================
# Docker Compose kullanıyorsanız, bu değerler docker-compose.yml'de override edilebilir
# Local development için yukarıdaki değerleri kullanın
# Docker için docker-compose.yml içindeki environment bölümünü düzenleyinw
